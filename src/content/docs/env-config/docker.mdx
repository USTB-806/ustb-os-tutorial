---
title: Docker 环境配置
description: 环境配置指导.
---

import { Tabs, TabItem } from '@astrojs/starlight/components';


:::note
如果想要详细了解更多关于Docker的内容可以参考 [Docker 官方文档](https://docs.docker.com/) 和 [Linux 101 - Docker](https://101.lug.ustc.edu.cn/Ch08/).

同时也建议去看 bilibili 上诸如《XX分钟的Docker实战攻略》这类实操视频，这有助于从整体上了解docker，当然，如果你本来就知道，也就无所谓了。
:::

## Docker 下载与安装

<Tabs>
    <TabItem label="Windows">

检查 BIOS 是否已经开启虚拟化，可以在任务管理器中查看. 然后在 [Docker 官网](https://docs.docker.com/get-docker/) 下载安装.

    </TabItem>
    <TabItem label="Linux">

以下是命令的所有过程，我们已经在ubuntu 22.04 上验证成功。

```bash
#安装前先卸载操作系统默认安装的docker，
sudo apt-get remove docker docker-engine docker.io containerd runc

#安装必要支持
sudo apt install apt-transport-https ca-certificates curl software-properties-common gnupg lsb-release

# 阿里源（推荐使用阿里的gpg KEY）
curl -fsSL https://mirrors.aliyun.com/docker-ce/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

#阿里apt源
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://mirrors.aliyun.com/docker-ce/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

#更新源
sudo apt update
sudo apt-get update

#安装最新版本的Docker
sudo apt install docker-ce docker-ce-cli containerd.io
#等待安装完成

#查看Docker版本
sudo docker version

#查看Docker运行状态
sudo systemctl status docker
```

    </TabItem>
    <TabItem label = "MacOS">

#### 一. 官网下载 dmg 包安装
- 进入官网 https://www.docker.com/
- 点击 Download Docker Desktop
- 根据芯片选择对应版本(Apple Silicon 或 Intel)下载并安装
- 在应用程序中启动Docker图形化界面 

#### 二. 使用 Homebrew 安装

在终端输入以下命令：
```bash
# 1.安装Homebrew,如果已经安装,请忽略下面这条命令：
# 官方源:
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 若下载失败,可使用国内镜像源:
/bin/bash -c "$(curl -fsSL https://gitee.com/ineo6/homebrew-install/raw/master/install.sh)"

# 2. 检查并更新软件源 
brew update

# 3. 下载并安装 Docker
brew install --cask docker

# 4. 启动 Docker 
open /Applications/Docker.app

# 等待顶部菜单栏出现鲸鱼图标...

# 5. 验证安装结果
# 查看Docker版本（应显示Client和Server两个部分，没有Server说明Docker未启动）
docker version
```
    </TabItem>
</Tabs>



